//tipos de dados primitivos
//strings, number, undefined, null, boolean

const nome = 'Nageysiel'; //string
const nome1 = "Nageysiel";//string
const nome2 = `Nageysiel`; //string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; //undefined = não aponta para local nenhum na memória
const sobreNomeAluno = null; //Nulo -> não aponta para local nenhum na memória
const booblean = false; //true ou folse (valor lógico)

//se quiser ver o tipo de cada variável
console.log(typeof nome);